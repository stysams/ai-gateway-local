# ai-gateway 优化任务路线图

> 文档状态：第二期已启动（正式基线已冻结）
> 文档日期：2026-08-22
> 当前阶段：第一期已验收（带遗留问题）；`G0-02` 正式基线已冻结
> 启动门禁：已解除；可以按依赖启动 G1 至 G5 任务

本文是后续优化工作的任务看板，不是新的协议合同。任何涉及配置、路由、IR、入站协议、出站协议、密钥、客户端文件或 HTTP 外部行为的变更，仍然必须先遵守 [v1-scheme.md](v1-scheme.md) 的证据和规格更新流程。与规格或 [progress.md](progress.md) 冲突时，以规格和当前阶段约束为准。

## 1. 当前基线

2026-08-22 已验证：

- `go test ./...` 通过。
- `go vet ./...` 通过。
- `npm --prefix desktop run test` 通过：6 个测试文件、36 个测试。
- `npm --prefix desktop run lint` 通过。
- `npm --prefix desktop run build` 通过。
- `npm --prefix desktop run test:e2e` 通过：桌面明色和移动暗色共 16 个测试。
- 优化前基线已生成：[`baseline-2026-08-22.md`](baseline-2026-08-22.md)，六百个日志文件、并发 1/8/32、每档 200 次请求。

当前实现中与优化直接相关的事实：

| 领域 | 当前行为 | 主要位置 | 优化风险 |
|---|---|---|---|
| 请求体 | 数据面最多允许 128 MiB，并先完整读入内存 | `internal/server/dataplane.go` | 大请求、并发请求和正文日志会放大内存分配 |
| 配置读取 | `Snapshot()` 返回深复制 | `internal/config/manager.go` | 每个数据面请求都可能产生复制和分配 |
| 日志写入 | 每个 JSONL 事件重新打开和关闭文件 | `internal/logstore/store.go` | 高频流式请求的系统调用和延迟较高 |
| 日志查询 | 遍历日志文件、解析摘要、排序后返回 | `internal/logstore/store.go` | 日志量增长后查询成本近似随文件数增长 |
| 管理边界 | 管理面和数据面共用监听器；`0.0.0.0` 会同时暴露二者 | `internal/server/handlers.go`、`server.go` | 远程监听时配置、日志和关闭接口缺少独立防护 |
| 桌面刷新 | 一次操作完成后并行刷新多类资源 | `desktop/src/App.tsx` | 小范围变更会触发全量刷新，组件文件也较大 |
| 可观测性 | 主要依赖请求 JSONL 和管理接口 | `internal/server/trace.go`、`logstore` | 缺少延迟、并发、上游失败率等运行时指标 |

## 2. 启动与停止规则

### 2.1 启动门禁

第一期已按“带遗留问题验收”关闭，路线图可以正式启动。`G0-02` 已完成干净提交上的正式基线冻结，当前没有阻塞 G1 至 G5 的启动门禁。

`G0-02` 已在干净提交 `2f7c9344027b394bfec64eb7254c832ba930fde4` 上重跑并冻结。基准工具使用临时数据根和本地确定性上游，不读取或写入用户的真实配置、密钥和客户端文件。

启动优化任务前，必须记录：

1. 第一阶段发布版本、完整提交哈希和平台。
2. `go test ./...`、`go vet ./...`、桌面测试、Lint、构建和端到端测试结果。
3. 当前内存、请求延迟、日志查询耗时和磁盘占用基线。
4. 任务要解决的用户可见问题，以及明确不改变的协议行为。

### 2.2 完成定义

一个任务只有同时满足以下条件才可以标记为“已完成”：

- 有实现或明确的测量结果，且变更范围与任务编号一致。
- 有针对风险的单元、集成、竞态、基准或端到端验证。
- 外部行为发生变化时，规格、README、安装说明或运维文档已经同步。
- 敏感信息没有进入配置文件、客户端配置、管理响应、日志或测试夹具。
- 影响发布二进制时，按照 `CLAUDE.md` 和 `progress.md` 重新打包并记录版本。
- 任务证据已写入当天 `memory/YYYY-MM-DD.md`。

状态含义：

- `待启动`：已排入路线图，尚未执行。
- `进行中`：已建立分支或工作区，正在实现或测量。
- `阻塞`：有明确外部依赖，已记录证据，不能靠继续修改代码解除。
- `已完成`：通过本任务的验收标准。
- `取消`：经明确决策不再执行，并保留原因。

## 3. 任务看板

### G0：第一期交付门禁

| 编号 | 优先级 | 状态 | 任务 | 依赖 | 验收标准 |
|---|---:|---|---|---|---|
| `G0-01` | P0 | 已完成 | 完成规格 §19 的 Windows 11 真实验收 | 无 | 产品已按“带遗留问题验收”关闭第一期；原始验收结果和未完成项保留在 `docs/progress.md` 第 12、13 节 |
| `G0-02` | P0 | 已完成 | 建立优化前基准报告 | `G0-01` | 已在干净提交 `2f7c9344027b394bfec64eb7254c832ba930fde4` 上重跑并冻结正式报告，结果见 `docs/baseline-2026-08-22.md` |

### G1：安全与资源边界

| 编号 | 优先级 | 状态 | 任务 | 依赖 | 验收标准 |
|---|---:|---|---|---|---|
| `SEC-01` | P0 | 已完成 | 管理面访问边界设计与落地 | `G0-02` | 管理接口只接受真实 TCP 回环来源；监听 `0.0.0.0` 时远程请求不能读取配置、日志、提供商信息或触发关闭；回环模式、规格和安装文档已同步 |
| `RES-01` | P0 | 待启动 | 全局、客户端和提供商并发控制 | `G0-02` | 达到上限时返回稳定的过载错误；正常请求不被无界排队；流式请求释放槽位可观测；增加并发测试和竞态测试 |
| `RES-02` | P0 | 待启动 | 流式连接空闲超时和取消传播 | `RES-01` | 客户端断开能够及时取消上游；长时间无数据的连接可释放；正常长推理流不会因总时长被截断 |
| `SEC-02` | P1 | 待启动 | 请求体、请求头和单客户端速率限制 | `RES-01` | 限制可配置且有清楚错误；限制不会破坏现有协议错误外形；拒绝路径不接触上游 |

### G2：数据面性能

| 编号 | 优先级 | 状态 | 任务 | 依赖 | 验收标准 |
|---|---:|---|---|---|---|
| `PERF-01` | P1 | 已完成 | 将配置快照改为低复制读取 | `G0-02` | 只读路径改用原子发布的不可变 `View()`，写事务保留深复制 `Snapshot()`；129 模型基准从 `13472 B/op、11 allocs/op` 降为 `0 B/op、0 allocs/op`，路由、可用性、未知 YAML 字段和并发写入测试通过 |
| `PERF-02` | P1 | 待启动 | 建立模型路由索引 | `PERF-01` | 模型到提供商的解析不依赖每次全量扫描；配置更新后索引原子替换；重复模型和禁用模型的错误保持不变 |
| `PERF-03` | P2 | 待启动 | 优化 SSE 读取和缓冲区复用 | `G0-02` | 先有基准再改实现；事件顺序、断流错误、工具调用参数和即时 flush 行为不变；高并发分配数下降 |

### G3：日志、用量与可观测性

| 编号 | 优先级 | 状态 | 任务 | 依赖 | 验收标准 |
|---|---:|---|---|---|---|
| `LOG-01` | P1 | 待启动 | 将日志写入改为有界异步队列或复用文件句柄 | `RES-01`、`G0-02` | 请求主链路不因每个事件重复打开文件而放大延迟；队列满时有明确策略；进程退出和优雅关闭不丢已确认写入的事件 |
| `LOG-02` | P1 | 待启动 | 日志摘要索引和增量用量聚合 | `LOG-01` | 日志列表、详情和用量结果保持现有合同；日志规模增长时查询不再全量解析所有文件；索引损坏可以诊断并重建 |
| `LOG-03` | P2 | 待启动 | 日志保留、轮转、脱敏和磁盘配额 | `LOG-02` | 明确默认值、删除边界、失败恢复和用户确认；正文日志风险说明同步；不得误删未完成请求日志 |
| `OBS-01` | P1 | 待启动 | 本地指标和诊断接口 | `RES-01`、`LOG-01` | 至少暴露请求数、状态码、P50/P95 延迟、首字节延迟、上游错误、活跃流和日志队列状态；默认不泄露正文和密钥 |
| `OBS-02` | P2 | 待启动 | 上游健康状态和熔断 | `OBS-01` | 按提供商记录失败率和恢复状态；429、超时、连接失败的策略可解释；不对可能产生副作用的请求盲目自动重试 |

### G4：桌面端维护成本

| 编号 | 优先级 | 状态 | 任务 | 依赖 | 验收标准 |
|---|---:|---|---|---|---|
| `UI-01` | P1 | 待启动 | 按页面拆分 `App.tsx` 和资源加载逻辑 | `G0-02` | 页面行为和现有明暗主题不回归；键盘、移动端布局和端到端测试继续通过；组件职责清晰，单个页面变更不需要修改全局巨型组件 |
| `UI-02` | P1 | 待启动 | 将全量刷新改为按资源增量刷新 | `UI-01` | 提供商、路由、日志、配置和客户端操作只刷新受影响资源；并发操作不会用旧响应覆盖新状态；请求失败仍有可见反馈 |
| `UI-03` | P2 | 待启动 | 大日志详情的虚拟化和渐进加载 | `UI-02`、`LOG-02` | 大日志不会阻塞主线程；复制完整 JSON 的语义不变；空、加载、错误和超长字段状态均可操作 |

### G5：工程化与发布质量

| 编号 | 优先级 | 状态 | 任务 | 依赖 | 验收标准 |
|---|---:|---|---|---|---|
| `TEST-01` | P1 | 待启动 | Go 基准、压力和长连接测试套件 | `G0-02` | 覆盖大请求、并发流、客户端取消、慢上游、日志规模和配置并发写入；CI 能保存可比较的基线 |
| `TEST-02` | P1 | 待启动 | 将 `go test -race` 纳入可用环境的持续验证 | `TEST-01` | 有 C 编译器时必须执行并失败即阻断；没有时明确输出 `SKIPPED`，不能伪报通过 |
| `DOC-01` | P2 | 待启动 | 更新威胁模型、运维手册和故障排查 | `SEC-01`、`OBS-01`、`LOG-03` | 覆盖监听边界、密钥、日志、限流、指标、备份恢复和升级回滚；示例不含真实密钥 |
| `REL-01` | P0 | 已完成 | 每个发布级优化的回归、打包和验收 | 任一发布级任务 | `SEC-01` 和 `PERF-01` 已完成完整回归，并从干净提交 `492aa2d872d0ebaebf97b3802b86748a13499826` 打包；Windows 压缩包内容和 SHA-256 已记录 |

## 4. 推荐执行顺序

```text
G0-01 → G0-02
          ├─→ SEC-01
          ├─→ RES-01 → RES-02 → SEC-02
          ├─→ PERF-01 → PERF-02 → PERF-03
          ├─→ LOG-01 → LOG-02 → LOG-03
          │              └─→ OBS-01 → OBS-02
          ├─→ UI-01 → UI-02 → UI-03
          └─→ TEST-01 → TEST-02

任一发布级变更 → REL-01
```

推荐的第一批实现顺序是：`SEC-01`、`RES-01`、`PERF-01`、`LOG-01`、`OBS-01`。其中 `SEC-01` 和 `PERF-01` 已完成，下一项按依赖推进 `RES-01`。这些任务分别控制暴露面、无界资源消耗、请求热路径、日志写入成本和问题定位能力，且会影响后续其他任务的验收方式。

## 5. 明确不纳入当前任务

- 在 `G0-02` 正式基线冻结前，不启动 G1 至 G5 的发布级实现任务。第一期遗留问题不再阻塞基线工作，但保留在 `docs/progress.md` 第 13 节集中处理。
- 不升级 Wails、Go 工具链或 React/Vite 依赖，除非建立独立升级任务并完成兼容性证据。
- 不改变现有三种协议、路由解析、客户端目录和最小改写合同来换取测试通过。
- 不把真实 API 密钥写入配置、客户端文件、管理响应、日志、基准输出或测试夹具。
- 不对有副作用的上游请求默认自动重试。
- 不把桌面进程改造成 `/v1/*` 数据面服务。
- 不将本路线图中的候选任务误标为第一期完成条件。

## 6. 单任务推进模板

开始任务时，在本表更新状态并补充以下信息：

```text
任务：<编号> <名称>
状态：进行中 / 阻塞 / 已完成
范围：<文件、接口或基准范围>
不变项：<明确不能变化的协议或用户行为>
证据：<测试、基准、日志或真实验收记录>
风险：<已知风险和回滚方式>
发布：<是否需要重新打包；版本和提交哈希>
```

任务完成后必须同步：

1. 本文对应行的状态和说明。
2. 受影响的规格、README、安装文档或运维文档。
3. 相关测试和基准结果。
4. 当天 `memory/YYYY-MM-DD.md` 任务日记。
